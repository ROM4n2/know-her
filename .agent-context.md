# 🤖 Agent Context: know-her

### 1. 🛠️ Ground Truth Verification Baseline
- **Primary Test Command**: `pnpm test`
- **Sub-check Commands**:
  - Typecheck: `pnpm run check` (`astro check`)
  - Graph & Hygiene: `pnpm run test:graph` (`python scripts/test_decision_tree.py && python scripts/test_glossary.py && python scripts/test_tools.py && python scripts/test_article_hygiene.py`)
  - Schema Curation: `pnpm run curate:check` (`python scripts/curate.py check`)
  - Static Build: `pnpm run build` (`astro build`)
- **Baseline Test Result**: ✅ 28 files typechecked (0 errors, 0 warnings), 4 graph/hygiene suites passed (Decision Tree 6 nodes/12 outcomes connected, Glossary 25 terms verified, Tools 4 components validated, Article Hygiene 23 articles clean), 22 articles schema-valid, 32 static pages built + Pagefind indexed (Captured on 2026-09-26).

### 2. 🗺️ Core Symbol & Architecture Map
- **Framework & Config**: Astro 7.3.3 + Tailwind CSS v4 (`@tailwindcss/vite`) + TypeScript strict (`astro.config.mjs`, `tsconfig.json`)
- **Base URL Routing**: `base: '/know-her'` — all internal anchors and asset links MUST be prefixed with `${import.meta.env.BASE_URL}` to prevent 404s on GitHub Pages.
- **Entrypoints & Routes**:
  - `src/pages/index.astro`: 首页，整合 AmbientHero（极光帘幕 WebGL2 实时光幕）、置顶今日精选（DailyCard）与每日一答、分类筛选卡片流、随机漫游。
  - `src/pages/articles/[id].astro`: 科普长文详情页，含动态阅读进度条、循证分级徽标、原出处外链直达、结构化导读要点复制卡片、关联医学术语速查与勘误通道。
  - `src/pages/categories/[category].astro`: 分类静态聚合页（contraception, pleasure, body, intimacy）。
  - `src/pages/glossary/index.astro`: 两性健康与医学术语词典库（25 词条，A-Z 索引与实时搜索）。
  - `src/pages/guide/decision-tree.astro`: 纯前端离线应急决策树（DecisionGuide）。
  - `src/pages/tools/index.astro`: 实用健康工具箱（72h/120h 紧急避孕倒计时、COC 漏服补救计算器、月经健康评估、性唤起油门刹车盘点器）。
  - `src/pages/how-we-source.astro` & `src/pages/privacy.astro`: 信任两页（信源准入与 Tier A/B/C 循证分级，零隐私收集与本地数据一键清空）。
  - `src/pages/rss.xml.ts`: RSS 2.0 订阅源。
- **Core Components & Layouts**:
  - `src/layouts/Base.astro`: 全站基底布局，注入 Paper & Ink 调色盘、全局 Meta、统一导航、统一免责声明页脚、44px 黄金触控区移动端吸底操作栏。
  - `src/components/AmbientHero.astro`: 原生 WebGL2 + FBM 算法呼吸感有机光幕（支持触控微摇曳、离屏暂停渲染与降级）。
  - `src/components/DailyCard.astro`: 基于积日算法的确定性轮换推荐卡片 + 30 秒知情速测打卡。
  - `src/components/DecisionGuide.astro`: 纯前端状态机决策树。
  - `src/components/tools/`: 4 大离线健康工具组件。
- **Data Collections & Models**:
  - `src/content.config.ts`:
    - `articles`: Zod 校验 `title`, `pubDate`, `summary`, `category`, `tags`, `source_url`, `source_name`, `evidence_tier` ('A'|'B'|'C'), `is_full_text`。
    - `glossary`: Zod 校验 `term`, `en_term`, `aliases`, `letter`, `definition`, `category`, `source`, `related_article`。
  - `src/data/decisionTree.ts`: 应急决策树状态图与行动方案。
  - `src/data/dailyQuiz.ts`: 每日知情速测题库（22 题闭环映射）。

### 3. 🚨 Local Engineering Red Lines & Gotchas
- **Zero Privacy Collection & Pure Static**: 全静态站点，零数据库，零个人隐私追踪，无后端服务器。
- **Article Hygiene Gate (`test_article_hygiene.py`)**: MDX 文章仅作为纯内容存在，严禁在正文中内嵌或导入全局布局组件（如 `MedicalDisclaimer`、`<Base`、`MobileBottomBar` 等，已由 `Base.astro` 统一管理）。
- **Foreign Key Integrity**: 工具组件与词典条目引用的 `related_article` 必须在 `src/content/articles/*.mdx` 真实存在，受 `test_tools.py` 与 `test_glossary.py` 自动化拦截。
- **External Links Authenticity**: 严禁捏造或凭记忆脑补第三方机构 URL；所有收录必须通过 `python scripts/curate.py check-links` 在线探测 200 OK。
- **Cross-Platform / Windows & CI Compatibility**:
  - 依赖锁定 `typescript@5.8.3`，禁止升级至破坏 `@astrojs/check` 的 TS 7.x。
  - Python 脚本仅使用标准库，CI 中 `setup-python` 禁止开启 `cache: "pip"`。
- **Visual Design Identity**:
  - Paper & Ink 色调（`--paper: #FAF8F5`, `--ink: #15140F`, `--accent: #F43F5E`）。
  - 严格去彩色系统 Emoji，全站统一采用单色几何 SVG、结构化字符标识（`[ 准入清单 ]`）与 Academic Modern Editorial 排版。
  - 实体墨线与按压触感（`shadow-[2px_2px_0_rgba(21,20,15,1)]`，`:active:translate`）。

### 4. 📚 Coding Vault Linkage
- Global Standards: `d:\Obsidian\Coding\AGENTS.md`
- Vault Search: `python d:\Obsidian\Coding\scripts\search-vault.py "<query>"`
- Workmemory Protocol: `WORKMEMORY/PROTOCOL.md` & `WORKMEMORY/PROJECT_OVERVIEW.md`
